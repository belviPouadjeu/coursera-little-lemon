import { BookingForm } from "../components"

const Reservation = () => {
    return (
        <main className='home-reservation'>
            <BookingForm  />
        </main>
    )
}

export default Reservation